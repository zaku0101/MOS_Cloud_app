<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<link rel="shortcut icon" href="/static/images/mos.png">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.9.1/font/bootstrap-icons.css">
	<link rel="stylesheet" type="text/css" href="/static/css/index.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
	<title>MOS_CLOUD</title>
    <style>
    body{
        font-family: 'Press Start 2p';font-size: 18px;
        background-color: black;
        color:whitesmoke;
		max-width: 1000px;
		text-align: center;
		margin-left: auto;
		margin-right: auto;
		
    }
	main{
		text-align: center;
	}
	a{
		color:whitesmoke;
	}
	h1{
		text-align: center;
		font-size: 15px;

	}
	td{
		text-align: center;
		font-size: 15px;
	}
	#filename{
		text-align: left;

	}
	.table{
		padding: 10px;
	}
	#name{
		text-align: left;
	}
	table {
	padding-left: 10px;
	padding-right:10px ;
	table-layout:fixed; 
	width:100%;
	
	}
table td, th {word-wrap:break-word;}
th {text-align: center;}
#size{
	text-align: right;
}
    </style>
</head>
<body>
<main>
<h1>File explorer</h1>
<hr>
<div class="table">

<table class="table table-striped table-responsive">
<table>
    <thead>
        <tr>
            <th id="name">Name</th>
            <th>Modified Time</th>
            <th id="size">Size</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>
                <a href="{{ url_for('get_files', req_path=data['parentFolder']) }}">
                    <span><i class="bi bi-folder-symlink"></i> cd ..</span>
                </a>
            </td>
            <td></td>
            <td></td>
        </tr>
        {% for fileObj in data['files'] %}
        <tr>
            <td id="filename">
                <a href="{{ url_for('get_files', req_path=fileObj['relPath']) }}">
                    <span><i class="{{fileObj['fIcon']}}"></i>{{ fileObj['name'] }}</span>
                </a>
            </td>
            <td id="time">{{fileObj['mTime']}}</td>
            <td id= "size">{{fileObj['size']}}</td>
        </tr>
        {% endfor %}
    </tbody>
</table>
</div>
</main>
</body>
